# 模块文档索引

代码路径 / 产品区域 → 模块文档。每条 PRD 交付后在对应模块文档里追加一节小节，不改写既有内容。

| 模块 | 覆盖范围 | 文档 |
|---|---|---|
| voipms-mcp | 整个仓库：VOIP.ms sub-account 查找 / 读取、语音信箱列表、语音信箱转接更新这四个 MCP 工具 | [voipms-mcp.md](voipms-mcp.md) |

未覆盖：`mcp-service-gateway/vendor/registry.yaml` 里 `voipms` 条目的登记状态——那是另一个仓库的模块文档职责范围，本仓库不记录它。
